#!/usr/bin/env node

import { main } from '../src/cli.js';

main().then(code => {
  if (typeof code === 'number') {
    process.exit(code);
  }
}).catch(err => {
  console.error('Fatal error:', err);
  process.exit(1);
});
